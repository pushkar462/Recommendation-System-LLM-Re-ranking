# Core ML — CPU-only PyTorch (saves ~700 MB vs full torch)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0
numpy>=1.24

# Neural re-ranker context encoding
# sentence-transformers loads ~80 MB model; guarded by RERANKER_USE_KEYWORDS_ONLY=1
# on Render free tier. Keep installed so paid tiers can use it.
sentence-transformers>=2.2

# API server
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
pydantic>=2.0

# Utilities
tqdm>=4.0
